# IREB CPRE Foundation Level 個人学習Webアプリ 実装計画

## 1. 目的と前提

本プロジェクトは、IREB CPRE Foundation Level を**英語で受験する学習者**が個人で学習するための、非公式・非営利の静的Webアプリを構築する。公式ソースは英語版だけを採用する。問題文、設問指示、選択肢、True/Falseのラベルは英語とし、学習解説は日本語を基本として、試験で認識すべき英語キーワードを常に併記する。模擬試験中は問題文や選択肢の日本語訳を表示しない。

情報源は `sources/manifest.json` に明示した IREB 公式資料だけに限定する。外部ブログ、市販教材、講座資料、非公式用語集、第三者作成問題は参照しない。アプリは IREB の公式教材・公式試験・認定トレーニングを代替するものではなく、IREB e.V. との提携や承認を示唆しない。

この文書は実装前の計画であり、この段階ではアプリ本体、教材データ、ダウンロード済み資料を追加しない。

## 2. 公式ソースの範囲

実装開始時点の候補は、IREB 公式 [Download Center](https://cpre.ireb.org/en/downloads-and-resources/downloads) で公開されている次の英語資料とする。実際に使用する版、直接ダウンロードURL、公開日、SHA-256は `sources/manifest.json` で固定する。

| ソースID | 資料 | 計画時の版 | 使用目的 | 公開物への扱い |
|---|---|---:|---|---|
| `fl-syllabus-en` | CPRE Foundation Level Syllabus | 3.3.0 | 章構成、Educational Objective（EO）、Cognitive Level、重要用語、学習範囲 | EO番号、短い見出し、用語、出典情報を利用。説明文は独自に作成 |
| `fl-handbook-en` | CPRE Foundation Level Handbook | 1.3.2 | EOの理解、章ごとの概念関係、解説の事実確認 | 原文、図表、定義、例を転載せず、独自の日本語解説に再構成 |
| `cpre-glossary-en` | CPRE Glossary | 2.2.0 | 試験対象の英語用語と概念の整合確認 | 英語の用語名のみを基本とし、定義文は転載せず独自に説明 |
| `fl-exam-regulations-en` | Foundation Level & RE@Agile Primer Examination Regulations | 5.6.2 | 試験時間、問題数、問題形式、採点方式、合格基準の実装根拠 | 必要最小限の事実だけを表示し、例題本文は収録しない |
| `fl-practice-exam-en` | CPRE Foundation Level Practice Examination | 2025.09.11 | 公式形式との整合を人手で確認するための参照専用資料 | 抽出・問題生成・配布の対象外。問題、選択肢、解答、解説を収録しない |

版は計画作成時点の確認値であり、実装時には公式ページとPDF本体の表紙・版履歴を再確認する。公式ページの表示版とPDF内部の版が異なる場合は処理を停止し、理由を確認してから採用する。新しい版を自動採用してはならない。

## 3. 採用技術

- フロントエンド: Next.js App Router + React + TypeScript（vinext/Viteで静的ビルド）
- ルーティング: GitHub Pages のサブパスでも動作するハッシュベースのルーティング
- データ検証: JSON Schema とNode.jsスクリプトによるビルド時検証
- 単体テスト: Node.js test runner
- 表示確認: Next.jsの本番ビルドと主要な模擬試験導線の手動確認
- 静的解析: ESLint、Prettier、TypeScript `tsc --noEmit`
- パッケージ管理: npm。依存関係は `package-lock.json` で固定
- 永続化: 非公開GitHubリポジトリを正本とし、`localStorage` はオフラインキャッシュとして使用する

依存関係の正確な版は実装時に選定してロックする。PDF取得・抽出は Node.js で動く TypeScript スクリプトに統一し、アプリのビルド中に外部サイトへアクセスしない。

## 4. 予定リポジトリ構成

```text
.
├─ .github/
│  └─ workflows/
│     ├─ ci.yml                    # lint / typecheck / test / build
│     ├─ deploy-pages.yml          # GitHub Pages（任意に有効化）
│     └─ verify-sources.yml        # 手動・定期の公式ソース整合確認
├─ public/
│  └─ icons/                       # 自作または利用条件を確認したUI用素材のみ
├─ sources/
│  ├─ manifest.json               # 許可ソース、版、URL、SHA-256、利用区分
│  └─ manifest.schema.json
├─ data/
│  ├─ raw/                         # ダウンロード原本。Git対象外
│  ├─ extracted/                   # PDF/ZIPからの抽出・解析結果。Git対象外
│  └─ content/                     # 審査済みの独自教材データ。Gitにコミット
│     ├─ chapters.json
│     ├─ learning-objectives.json
│     ├─ terms.json
│     └─ questions/
│        ├─ eu-1.json
│        └─ ...
├─ scripts/
│  ├─ download-sources.ts          # manifest記載物だけを取得・ハッシュ検証
│  ├─ extract-pdfs.ts              # 章、EO、重要用語候補をローカル抽出
│  ├─ build-content-scaffold.ts    # 抽出結果から執筆用の空スキャフォールドを生成
│  ├─ validate-content.ts          # スキーマ、出典、網羅性、類似性を検査
│  └─ source-policy.ts             # 許可ホスト、MIME、サイズ等の安全規則
├─ src/
│  ├─ app/                         # ルート、画面構成
│  ├─ components/                  # 共通UI
│  ├─ features/
│  │  ├─ dashboard/                # 進捗サマリー
│  │  ├─ learn/                    # 章・EO・用語学習
│  │  ├─ practice/                 # 章別／EO別練習
│  │  ├─ mock-exam/                # 45問・75分
│  │  └─ review/                   # 間違い問題の復習
│  ├─ content/                     # 検証済みJSONの読み込み
│  ├─ domain/                      # 問題、採点、進捗の純粋ロジック
│  ├─ storage/                     # localStorage、移行、破損復旧
│  ├─ styles/                      # モバイルファーストのスタイル
│  └─ test/
├─ tests/
│  ├─ fixtures/                    # 自作の最小PDF/JSONテストデータ
│  └─ e2e/
├─ .gitignore
├─ LICENSE
├─ README.md
├─ PLAN.md
├─ package.json
├─ package-lock.json
├─ tsconfig.json
├─ vite.config.ts
└─ vercel.json                     # Vercel向け静的配信設定
```

`data/raw/**` と `data/extracted/**` は `.gitignore` で除外する。例外用の否定パターンは置かず、CIで追跡ファイルに両ディレクトリ配下が存在しないことも検査する。公式PDF、公式ZIP、抽出した全文、ページ画像、公式図表は `public/` やビルド成果物にも含めない。

## 5. `sources/manifest.json` の方針

各エントリは少なくとも次を保持する。

```json
{
  "id": "fl-syllabus-en",
  "title": "CPRE Foundation Level Syllabus",
  "publisher": "IREB e.V.",
  "language": "en",
  "version": "3.3.0",
  "publishedAt": "2026-04-01",
  "sourcePageUrl": "https://cpre.ireb.org/en/downloads-and-resources/downloads#cpre-foundation-level-syllabus",
  "downloadUrl": "https://cockpit-v1.ireb.org/...pdf",
  "mediaType": "application/pdf",
  "sha256": "<64桁の小文字16進数>",
  "usage": "extract",
  "enabled": true
}
```

`usage` は次のいずれかに限定する。

- `extract`: 章、EO、重要用語候補のローカル抽出を許可する
- `rules-only`: 試験仕様など必要な事実の確認だけに使用する
- `reference-only`: 人手での確認だけに使用し、抽出・問題生成には渡さない

安全上の規則は次のとおりとする。

1. `scripts/download-sources.ts` は manifest の `enabled: true` のURLしか取得しない。任意URLのコマンドライン指定は受け付けない。
2. HTTPS と明示的な IREB 公式ホストだけを許可し、リダイレクト後のホストも再検査する。
3. MIMEタイプ、拡張子、最大サイズ、タイムアウトを検査し、一時ファイルへのストリーム完了後に原子的に配置する。
4. 通常実行ではSHA-256が一致したファイルだけを `data/raw` に残す。不一致、版の不一致、URL切れは失敗とし、自動更新しない。
5. 新規版の採用時だけ `--propose` モードで観測したSHA-256を標準出力に提示する。このモードも manifest 記載URLだけを取得し、manifest自体は書き換えない。版、URL、ハッシュの変更は人が確認した1つのレビュー可能な差分としてコミットする。
6. 同一IDの版を更新するときは、旧版との差分概要と教材への影響を確認してから生成物を再審査する。
7. `verify-sources.yml` は固定URLのハッシュ検証と公式ダウンロードページ上の表示版チェックを行うが、変更をコミットしない。変化を検知した場合は失敗として通知する。

## 6. データ処理パイプライン

### 6.1 取得

`npm run sources:download` で manifest とスキーマを検証し、許可された資料だけを `data/raw/<source-id>/<version>/` に取得する。取得ログにはソースID、版、最終URL、バイト数、SHA-256、取得日時を残すが、ログもGitにはコミットしない。

### 6.2 PDF抽出

`npm run sources:extract` は `usage: extract` のPDFだけを対象に、ページ単位のテキストと位置情報を `data/extracted/<source-id>/<sha256>/` に出力する。抽出対象は次の構造候補に限定する。

- EU／章／節の番号と短い見出し
- `EO x.y.z` の識別子、L1〜L3のCognitive Level、所属章
- 章冒頭の重要用語名
- 出典追跡用のページ番号と文字位置

PDF全文の抽出結果は公開データではなく、一時的な執筆・検証材料である。ヘッダー／フッター、目次、改ページのハイフンなどを正規化しても、原文を `data/content` へ自動コピーしない。レイアウト変更で抽出件数や章対応が変わった場合は、期待スナップショットとの差分を出して処理を止める。

### 6.3 正規化と執筆スキャフォールド

`build-content-scaffold.ts` は抽出結果から、EO番号、章番号、用語名、出典候補だけを持つ空の執筆用JSONを `data/extracted` に生成する。日本語解説、例、設問、選択肢、誤答理由を自動的に埋めて公開する処理は作らない。

執筆者は公式資料を理解した上で、内容をいったん概念単位に分解し、自分の言葉で日本語解説と独自例を作成する。必要な英語は試験キーワード、固有のEO番号、短い章見出しに限定する。完成候補はレビューを通過した後だけ `data/content` に移す。

### 6.4 公開コンテンツ検証

`validate-content.ts` で少なくとも次を検査する。

- すべての章、EO、用語、問題が存在する manifest のソースID、版、章／節を参照している
- 全EOが少なくとも1つの解説に対応し、問題バンクの各問題が1つ以上のEOに対応している
- 英語キーワードの表記が用語マスターと一致している
- 問題ID、選択肢ID、出典、正答が一意で、正答数と問題形式が矛盾しない
- 公式原文との長い連続一致や高い類似度を検出して失敗させる
- 公式模擬問題由来のデータが問題生成処理に入力されていない
- 公開JSONにPDF抽出本文、ページ画像、ファイルパスなどが混入していない

類似性検査は著作権判断の代替ではない。連続語句、n-gram、類似度の複数の警告を使い、最終的には人が原文と比較する。自動検査のしきい値未満でも、表現・例・設問構造が公式資料に過度に依存していれば不採用とする。

## 7. 公開データモデルと出典表示

主な公開データは次の形にする。

- `Chapter`: EU番号、短い英語見出し、独自の日本語概要、EO ID、用語ID、出典参照
- `LearningObjective`: EO ID、Cognitive Level、所属章、独自の日本語要約、英語キーワード、出典参照
- `Term`: 英語用語、日本語の短い対訳、独自の日本語説明、混同しやすい点、出典参照
- `Question`: 独自の英語の設問指示・問題文・選択肢、形式、配点、正答、独自の日本語解説、各選択肢の理由、EO ID、難度、出典参照
- `SourceRef`: source ID、資料名、版、章／節、任意のページ番号、公式資料ページURL

学習画面と回答後の解説には、最低でも「資料名 / 版 / 章または節」を表示する。詳細を開くとEO番号、ページ番号、公式ダウンロードページへのリンクを確認できるようにする。出典は、その説明が公式文章の引用であることではなく、独自解説の根拠と追跡先を示す。

版情報はコンテンツ側に自由記述で重複させず、source IDを通じて manifest の公開可能なメタデータから解決する。これにより、古い版の参照が混在した場合にビルドを失敗させられる。

## 8. 著作権に配慮したコンテンツ生成方針

### 8.1 許容する内容

- 資料名、版、章／節番号、EO番号、Cognitive Levelなどの書誌・識別情報
- 学習に必要な短い英語キーワードや用語名
- 公式資料の概念を理解した上で独自に構成・表現した日本語解説
- 公式資料にない架空の業務・システム・状況を使った独自例
- EOで求められる知識・理解・適用能力を測るためにゼロから作った英語問題
- 必要最小限の短い引用。引用符、出典、ページを明記し、引用がなくても説明できる場合は引用しない

### 8.2 禁止する内容

- 公式資料の段落、定義、箇条書き、表、図、テンプレート、スクリーンショットの転載
- 長い原文の日本語訳や、語順だけを変えた近接パラフレーズ
- 章全体を追えるほど詳細な逐語的再構成
- 公式模擬問題の問題文、選択肢、解答、解説、固有シナリオの転載または軽微な改変
- 公式問題の選択肢だけを入れ替えた問題や、数値・固有名詞だけを置換した問題
- IREB以外の資料から持ち込んだ説明、用語定義、例題
- AI生成物を、出典照合と人手レビューなしで公開データに採用すること

### 8.3 生成・レビュー手順

1. 1コンテンツにつき対象EO、Cognitive Level、公式資料の章／節を先に固定する。
2. 原文を閉じた状態でも説明できる粒度まで概念を整理し、日本語で新しい構成と表現を作る。
3. 例や問題には公式資料と異なる独自シナリオを使う。設問指示、問題文、選択肢は英語、解説と誤答理由は日本語にする。英文は英語試験として自然かつ曖昧でない表現であることを別途レビューする。
4. 正答の根拠、誤答が誤りである根拠、Cognitive Levelへの適合を公式資料で照合する。
5. 類似性検査を通し、別のレビュー担当が原文・公式模擬問題との過度な類似がないことを確認する。
6. 出典、版、章／節を確定し、レビュー状態を `approved` にしてから公開データへ入れる。

AIを執筆補助に使う場合も、入力は manifest 対象資料からローカル抽出した必要最小限の範囲だけとし、外部検索やモデルの一般知識を根拠にしない。AI出力は草案として扱い、上記の照合とレビューを必須とする。

リポジトリとアプリには、非公式学習ツールであること、公式資料の著作権が各権利者に帰属すること、最新情報はIREB公式サイトで確認すべきことを明記する。

## 9. 学習機能

### 9.1 章・EO・用語学習

- ナビゲーション、ボタン、設定、試験指示を含むUI全体は英語にする。日本語は回答後または復習時の解説本文に限定する
- EU 1〜7を一覧表示し、完了率と未学習数を表示する
- 章内でEO、Cognitive Level、重要英語用語、日本語解説、出典を関連づけて表示する
- 用語一覧は英語名で検索でき、日本語説明と該当章へ移動できる
- 学習済み、要復習、ブックマークを端末内に保存する

### 9.2 練習問題

- 章別、EO別、未回答、間違いのみで出題できる
- 設問指示、問題文、選択肢は英語で表示し、回答後に初めて日本語解説、英語キーワード、出典を表示する
- 学習・練習モードでも問題文や選択肢の逐語訳は原則表示せず、英語のまま意味を判断する練習を優先する
- 試験規則に合わせ、Single Choice、Multiple Choice、True/False statementsを扱う
- Multiple Choiceは要求選択数を明示し、過剰選択時の採点を試験規則に合わせる
- 180問以上の独自問題を用意し、全70 EOを各2問以上で網羅する。L1は用語認識、L2は比較と理由判断、L3は具体場面への適用を中心にする

### 9.3 45問・75分の模擬試験

- 45問を問題バンクから重複なく選び、75分のカウントダウンを行う
- 章の推奨学習時間、EO数、Cognitive Level、問題形式を基にしたアプリ独自の公開ブループリントで抽選する。IREB公式の非公開出題比率であるとは表示しない
- 試験中は日本語訳、日本語解説、正誤、用語ヒントを隠し、英語の問題、問題一覧、未回答、フラグ、残り時間だけを表示する
- 再読み込み後も締切時刻と回答を復元し、時間切れで自動提出する
- 公式規則に沿った1〜3点の配点と形式別採点を実装し、得点率と70%の目安を表示する
- 結果画面ではEU別、EO別、Cognitive Level別の正答率、誤答一覧、出典を表示する
- 「本番結果を保証しない非公式シミュレーション」であることを明記する

### 9.4 間違い問題の復習

- 誤答時に問題ID、選択回答、日時、対象EOを復習キューへ追加する
- 復習画面で未解決の誤答、再回答済み、ブックマークを絞り込む
- 再正解しても履歴を消さず、連続正解回数と最終回答日時を更新する
- 問題データの版が変わった場合は、古い回答を誤って新問題に適用しないよう `contentVersion` で区別する

## 10. ローカルストレージ設計

保存キーに名前空間とスキーマ版を持たせ、少なくとも次を保存する。

- 学習済み章／EO／用語
- 問題ごとの回答履歴、正誤、ブックマーク、復習状態
- 模擬試験の履歴、得点、所要時間
- 進行中の模擬試験の問題ID、回答、フラグ、締切時刻
- コンテンツ版とストレージスキーマ版

読み込み時にスキーマ検証し、旧版は明示的なマイグレーションを通す。破損データ、容量超過、ストレージ利用不可でもアプリ本体が停止しないようにする。設定画面から学習データの初期化とJSONエクスポート／インポートを提供する。個人情報、認証情報、公式資料本文は保存しない。

## 11. UI、モバイル、アクセシビリティ

- 320px幅から利用できるモバイルファースト設計とし、横スクロールを発生させない
- タップ領域、文字サイズ、行間、コントラストを確保する
- 問題文と選択肢はフォーム要素として実装し、キーボード操作、フォーカス表示、ラベル、スクリーンリーダーに対応する
- 色だけで正誤や進捗を伝えず、アイコンとテキストを併用する
- 模擬試験では残り時間を常時確認できるが、過度なアニメーションは使わない
- 主要画面をDashboard、Learn、Practice、Mock Exam、Review、About/Sourcesに絞る

## 12. テスト方針

### データ処理

- manifestの必須項目、許可ホスト、版形式、SHA-256形式
- 正常ダウンロード、ハッシュ不一致、リダイレクト先違反、MIME違反、途中失敗
- 自作の小さなPDF fixtureから章、EO、用語候補を抽出できること
- PDFレイアウト変化、抽出件数急変、重複IDを検出できること
- raw／extractedがGit追跡対象やビルド成果物に含まれないこと

### コンテンツ

- 全JSONのスキーマ、参照整合性、EO網羅性、出典表示情報
- 正答数、問題形式、配点、選択肢の整合性
- 設問指示、問題文、選択肢に日本語フィールドや日本語文字列が混入していないこと
- 長い原文一致、重複問題、極端に似た選択肢の検出
- 180問以上の在庫、全70 EOの複数問題、章・形式・難度別の偏りを検査すること

### アプリ

- 形式別採点、部分点、過剰選択、未回答、70%境界
- シード付き抽選で45問が重複せず、ブループリントを満たすこと
- タイマー、時間切れ提出、再読み込み復元
- 誤答キュー、再回答、ストレージ移行、破損復旧
- 主要画面のキーボード操作とアクセシブルネーム
- モバイル／デスクトップの主要E2E導線

## 13. CI/CDとデプロイ

`ci.yml` は pull request と `main` へのpushで次を実行する。

1. Node.jsをセットアップ
2. `npm ci`
3. `npm run lint`
4. `npm run content:validate`
5. `npm test`
6. `npm run build`
7. ビルド成果物に禁止ファイルや公式原文が含まれていないことを検査

通常のCI／buildはネットワークから公式資料を取得せず、コミット済みの審査済み `data/content` だけを使う。ソース取得・ハッシュ確認は、権限と目的を分離した `verify-sources.yml` の手動実行または低頻度の定期実行で行う。

同期版はサーバーAPIを必要とするため、SitesまたはVercelへデプロイする。GitHub Pagesはコードと静的プレビュー用途に限定する。デプロイはCI成功後だけ行う。

## 14. 実装フェーズ

1. **基盤**: Next.js/React/TypeScript、品質ツール、CI、Pages/Vercel設定、`.gitignore`
2. **ソース管理**: manifest／schema、公式URLと版の再確認、SHA-256固定、安全なダウンロード
3. **抽出**: PDF抽出、章／EO／用語候補の正規化、差分検出
4. **ドメインモデル**: 公開JSONスキーマ、出典参照、採点、模擬試験ブループリント
5. **教材**: 日本語解説、英語キーワード、180問以上の独自問題、全70 EO網羅、著作権・内容レビュー
6. **UI**: 学習、練習、模擬試験、復習、進捗、出典画面
7. **永続化**: localStorage、移行、復元、エクスポート／インポート
8. **検証**: 単体、コンポーネント、E2E、モバイル、アクセシビリティ、成果物監査
9. **公開準備**: README、非公式表記、ライセンス範囲、デプロイ手順、最終コンテンツレビュー

各フェーズはlint、typecheck、test、buildが成功する状態で完了させる。教材作成とUI実装を分離し、未審査コンテンツが公開ビルドへ入らない仕組みを先に作る。

## 15. 完了条件

- `sources/manifest.json` に記載された有効なIREB公式資料以外を処理できない
- すべての採用資料で版とSHA-256が固定され、不一致時に自動更新せず失敗する
- `data/raw` と `data/extracted` および公式ファイルがGit履歴・配布物に含まれない
- EU、EO、重要用語を追跡でき、各解説に資料名、版、章／節が表示される
- 英語版資料だけを根拠とし、日本語解説と英語キーワードが併記され、長文転載や公式問題の転用がない
- すべての設問指示、問題文、選択肢が英語で、模擬試験中に日本語訳や日本語ヒントが表示されない
- UI全体が英語で、日本語が表示されるのは回答後または復習時の解説だけである
- 180問以上の独自問題バンクから毎回45問を抽出し、75分の模擬試験を完走できる
- 誤答復習、学習進捗、進行中試験が非公開GitHubリポジトリへ保存され、localStorageからオフライン復元できる
- 主要機能がスマートフォン、キーボード、スクリーンリーダーで利用できる
- GitHub Actionsでlint、typecheck、test、content validation、buildが成功する
- GitHub PagesまたはVercelへ同一成果物をデプロイできる
- READMEとアプリ内に出典、非公式性、著作権、免責、データ保存方針が明記される

この計画のレビュー承認後に、フェーズ1から実装を開始する。

